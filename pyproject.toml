[project]
name = "handwrite-claudecode"
version = "0.1.0"
description = "A learning project for building a small Claude Code-style agent loop"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=1.2.0",
    "python-dotenv>=1.0.0",
]
